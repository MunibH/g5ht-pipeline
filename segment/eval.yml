name: eval
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - numpy
  - scipy
  - tifffile
  - pip
  - pip:
    - tensorflow[and-cuda]
    - tensorflow-text
    - keras
    - keras-hub